# Generated outputs

Running `python examples/run_example.py` writes a compact JSON summary here. Generated
outputs are intentionally ignored so repeated simulations do not make the repository
grow. The curated README figure is written to `docs/images/`.
